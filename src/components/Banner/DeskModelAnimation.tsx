import React, { useEffect, useState, useRef } from 'react';
import * as THREE from 'three';

const DeskModelAnimation = () => {
    const [loading, setLoading] = useState(true)

    return (
        <div>Desktop</div>
    );
};

export default DeskModelAnimation;